version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build --output-path=dist/mtsg-site
  artifacts:
    baseDirectory: dist/mtsg-site
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
